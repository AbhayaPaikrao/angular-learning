import { ToopTipDirective } from './toop-tip.directive';

describe('ToopTipDirective', () => {
  it('should create an instance', () => {
    const directive = new ToopTipDirective();
    expect(directive).toBeTruthy();
  });
});
